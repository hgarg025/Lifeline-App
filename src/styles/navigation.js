import colors from './colors';
export const transparentHeaderStyle = {
    backgroundColor: colors.green02,
    // position: 'absolute',
    // top: 0,
    // left: 0,
    // right: 0,
    borderBottomWidth: 0, 
}